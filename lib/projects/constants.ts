/**
 * Projects list page: max cards and max saved projects in this MVP workspace slice.
 * Keep in sync with `v_max_projects` / `v_max` in
 * `supabase/migrations/20260408120000_invitee_project_capacity.sql`.
 */
export const MAX_PROJECTS_ON_WORKSPACE_LIST = 4;
